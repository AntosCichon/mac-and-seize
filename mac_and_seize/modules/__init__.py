"""Feature modules.

Every subpackage here that exposes a top-level ``register() -> ModuleSpec`` is
auto-discovered at startup (see :mod:`mac_and_seize.core.plugins`). Drop a new
package in this directory to add a feature - no other file needs editing.

See ``README.md`` in this directory for the full authoring guide.
"""
